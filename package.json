{
  "name": "claudeway",
  "version": "0.12.0",
  "description": "Slack-to-Claude Code CLI gateway for solo developers",
  "type": "module",
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "build": "tsc",
    "lint": "eslint src/",
    "format": "prettier --write 'src/**/*.ts'",
    "format:check": "prettier --check 'src/**/*.ts'",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "sync-repos": "bun -e \"import { syncRepos } from './src/sync-repos.ts'; syncRepos();\"",
    "prepare": "husky"
  },
  "keywords": [
    "slack",
    "claude",
    "claude-code",
    "gateway",
    "cli"
  ],
  "author": "Tamas Kalman",
  "license": "MIT",
  "dependencies": {
    "@deepgram/sdk": "^5.0.0",
    "@slack/bolt": "4.6.0",
    "uuid": "13.0.0",
    "yaml": "2.8.2"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/bun": "1.3.9",
    "eslint": "10.0.2",
    "eslint-config-prettier": "10.1.8",
    "husky": "9.1.7",
    "lint-staged": "16.3.0",
    "prettier": "3.8.1",
    "typescript": "5.9.3",
    "typescript-eslint": "8.56.1"
  },
  "lint-staged": {
    "*.ts": [
      "prettier --write",
      "eslint --fix"
    ]
  }
}
